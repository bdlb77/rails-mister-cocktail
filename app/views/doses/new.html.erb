<h1>Add a Dose!</h1>

<%= simple_form_for(@cocktail, @dose) do |f| %>
  <%= f.label :description,  %>
  <%= f.input :description  %>
  <%= f.association :ingredients %>
  <%= f.button :submit %>
<% end %>
